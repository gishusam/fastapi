name: Build and Deploy code 

on: [push,pull_request]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      -name: checkout code
       uses: actions/checkout@v2
      -name: print hi
       run: echo "hi"
        